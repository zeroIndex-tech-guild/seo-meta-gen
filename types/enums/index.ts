export enum GENERATED_FOR {
  UI = 'ui',
  WEBHOOK = 'webhook',
}

export enum STATUS {
  SUCCESS = 'success',
  FAILED = 'failed',
}

export enum USER_TIER_STATUS {
  ACTIVE = 'active',
  EXPIRED = 'expired',
  CANCELLED = 'cancelled',
}
